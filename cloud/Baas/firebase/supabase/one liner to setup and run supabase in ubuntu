sudo apt-get update && sudo apt-get install -y curl gnupg && curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash - && sudo apt-get install -y nodejs && npm install -g supabase && supabase init && supabase start
