apt update && apt install -y w3m && curl -L https://fly.io/install.sh | sh && export FLYCTL_INSTALL="$HOME/.fly" && export PATH="$FLYCTL_INSTALL/bin:$PATH" && echo 'export FLYCTL_INSTALL="$HOME/.fly"' >> ~/.bashrc && echo 'export PATH="$FLYCTL_INSTALL/bin:$PATH"' >> ~/.bashrc && source ~/.bashrc && flyctl auth signup || (flyctl auth login && w3m https://fly.io/app/auth/cli/$(flyctl auth token))
