create one liner, to setup and run sqlcmd in ubuntu, and use it in the end! && sudo apt-get install mssql-tools -y && echo 'export PATH="$PATH:/opt/mssql-tools/bin"' >> ~/.bashrc && source ~/.bashrc && sqlcmd -?
