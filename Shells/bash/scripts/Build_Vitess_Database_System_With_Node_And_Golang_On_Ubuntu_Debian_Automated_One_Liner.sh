cd && getgo && getc && getmariadb && sudo apt-get install -y make unzip g++ etcd curl git wget ant maven default-jdk zip && curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.1/install.sh | bash && export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" && [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion" && nvm install --lts 16.13.0 && nvm use 16.13.0 && sudo service etcd stop && sudo systemctl disable etcd && git clone https://github.com/vitessio/vitess.git && cd vitess && export VTDATAROOT=/tmp/vtdataroot && export PATH=~/vitess/bin:${PATH} && make build
