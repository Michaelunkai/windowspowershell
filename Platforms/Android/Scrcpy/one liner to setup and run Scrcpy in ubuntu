sudo apt install ffmpeg libsdl2-2.0-0 adb meson wget gcc git pkg-config meson ninja-build libsdl2-dev libavcodec-dev libsdl2-dev cmake libavformat-dev libavdevice-dev libavformat-dev libavutil-dev libswresample-dev libusb-1.0-0 libusb-1.0-0-dev && sudo su -c "cd /opt && git clone https://github.com/Genymobile/scrcpy && cd scrcpy && ./install_release.sh && scrcpy --version"
