curl -sL https://run.linkerd.io/install | sh && export PATH=$PATH:$HOME/.linkerd2/bin && linkerd check --pre && linkerd install --crds | kubectl apply -f - && linkerd install --set proxyInit.runAsRoot=true | kubectl apply -f - && linkerd check
