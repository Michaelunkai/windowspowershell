create one liner, to setup and run slimbrowser in ubuntu, and use it in the end! && wget https://www.slimbrowser.net/releases/slimbrowser.tar.gz && tar -xvf slimbrowser.tar.gz && cd slimbrowser && ./sb
