Add the GitLab Repository as a Remote:
Add the GitLab repository as a remote origin. You can name it gitlab.

 
 
git remote add gitlab https://gitlab.com/michaelovsky5/study.git
Verify Remotes:
Verify that both remotes are set up correctly.

 
 
git remote -v
You should see something like this:

 
 
origin  https://github.com/michaelunkai/study.git (fetch)
origin  https://github.com/michaelunkai/study.git (pu )
gitlab  https://gitlab.com/michaelovsky5/study.git (fetch)
gitlab  https://gitlab.com/michaelovsky5/study.git (pu )
