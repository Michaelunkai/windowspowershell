<Window x:Class="GitDesk.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Width="400" Height="180"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgDarkBrush}"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TitleText" FontSize="16" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
        <TextBlock Grid.Row="1" x:Name="PromptText" FontSize="12"
                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
        <TextBox Grid.Row="2" x:Name="InputBox" Style="{StaticResource ModernTextBox}"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Style="{StaticResource ModernButton}" Click="Cancel_Click" Margin="0,0,8,0"/>
            <Button Content="OK" Style="{StaticResource AccentButton}" Click="OK_Click"/>
        </StackPanel>
    </Grid>
</Window>
