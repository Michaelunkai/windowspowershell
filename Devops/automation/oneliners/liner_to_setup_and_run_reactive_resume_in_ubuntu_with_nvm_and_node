nvm install 18 && nvm use 18 && git clone https://github.com/AmruthPillai/Reactive-Resume.git && cd Reactive-Resume || exit 1; npm install; docker-compose up -d; echo "Reactive Resume is running. Access it at http://localhost:3000"
