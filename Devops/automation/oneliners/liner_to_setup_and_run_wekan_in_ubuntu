getsnap && sudo snap install wekan && sudo snap set wekan root-url="http://$(hostname -I | awk '{print $1}'):3001" && sudo snap set wekan port='3001' && sudo systemctl restart snap.wekan.mongodb && sudo systemctl restart snap.wekan.wekan && echo "Wekan is running at: http://$(hostname -I | awk '{print $1}'):3001" && echo "Default login credentials: Username: admin, Password: admin" && gcl 3001
