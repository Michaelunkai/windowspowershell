mkdir -p ~/minio && cd ~/minio && wget https://dl.min.io/server/minio/release/linux-amd64/minio && chmod +x minio && ./minio server ~/minio-data && echo "MinIO is running. Access the web interface at http://localhost:9000"
