sudo apt-get update && sudo apt-get install -y openjdk-11-jre-headless && curl https://raw.githubusercontent.com/rundeck/packaging/main/scripts/deb-setup.sh 2> /dev/null | sudo bash -s rundeckpro && sudo systemctl daemon-reload && sudo service rundeckd start && tail -f /var/log/rundeck/service.log
