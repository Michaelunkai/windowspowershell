After running your application with the X-Ray SDK and the X-Ray daemon, you can view the traces in the X-Ray console:

Open the AWS Management Console.
Navigate to X-Ray.
You should see the traces generated by your application.
