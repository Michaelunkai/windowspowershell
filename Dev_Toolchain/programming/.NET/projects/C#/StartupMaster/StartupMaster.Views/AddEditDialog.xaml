<Window x:Class="StartupMaster.Views.AddEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Add New Startup Item"
        Width="550" Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource SystemControlPageBackgroundChromeLowBrush}">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Paste Full Path to Application" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Command/Path ONLY -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="File Path:" Margin="0,0,0,8" FontWeight="SemiBold" FontSize="14"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="CommandTextBox" Grid.Column="0" Height="40" Margin="0,0,10,0" 
                         FontSize="13"
                         ui:ControlHelper.PlaceholderText="Paste full path here (e.g., C:\Program Files\MyApp\app.exe)"/>
                <Button x:Name="BrowseButton" Grid.Column="1" Content="Browse..." Width="100" Height="40" Click="BrowseButton_Click"/>
            </Grid>
            <TextBlock Text="Everything else will be auto-configured for immediate startup (no delay)" 
                       Margin="0,8,0,0" FontSize="12" Opacity="0.7"/>
        </StackPanel>

        <!-- Hidden fields (auto-filled) -->
        <StackPanel Grid.Row="2" Visibility="Collapsed">
            <TextBox x:Name="NameTextBox"/>
            <TextBox x:Name="ArgumentsTextBox"/>
            <ComboBox x:Name="LocationComboBox" SelectedIndex="0">
                <ComboBoxItem Content="Registry (Current User)" Tag="RegistryCurrentUser"/>
            </ComboBox>
            <StackPanel x:Name="DelayPanel" Visibility="Collapsed">
                <Slider x:Name="DelaySlider" Value="0"/>
                <TextBlock x:Name="DelayValueText" Text="0s"/>
            </StackPanel>
            <TextBox x:Name="NotesTextBox"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="CancelButton" Content="Cancel" Width="110" Height="38" 
                    Margin="0,0,10,0" IsCancel="True" Click="CancelButton_Click"/>
            <Button x:Name="SaveButton" Content="Save" Width="110" Height="38" 
                    Style="{StaticResource AccentButtonStyle}" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
