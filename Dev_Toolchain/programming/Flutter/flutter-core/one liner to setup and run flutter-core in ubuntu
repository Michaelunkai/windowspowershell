sudo apt update && sudo apt install -y curl git unzip xz-utils && curl -O https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.10.5-stable.tar.xz && tar xf flutter_linux_3.10.5-stable.tar.xz && export PATH=$PATH:$(pwd)/flutter/bin && flutter --version
