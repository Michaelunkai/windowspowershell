sudo apt update && sudo apt install -y build-essential autoconf libtool pkg-config && git clone -b v1.39.0 https://github.com/grpc/grpc && cd grpc && git submodule update --init && make && sudo make install && sudo ldconfig && grpc_cpp_plugin --version
