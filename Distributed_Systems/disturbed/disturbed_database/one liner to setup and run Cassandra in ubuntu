echo "deb https://www.apache.org/dist/cassandra/debian 40x main" | sudo tee /etc/apt/sources.list.d/cassandra.list && wget -qO - https://www.apache.org/dist/cassandra/KEYS | sudo apt-key add - && sudo apt update && sudo apt install -y cassandra && sudo systemctl start cassandra
